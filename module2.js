// accepting variables with object
// const obj = require('./module1');
// console.log(obj.myName, obj.myAge, obj.sex);

// accepting variable with destructuring
const {myName, myAge, sex} = require('./module1');
console.log(myName, myAge, sex);